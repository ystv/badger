import { z } from 'zod';

export const MediaProcessingTaskScalarFieldEnumSchema = z.enum(['id','media_id','description','additionalInfo','state']);

export default MediaProcessingTaskScalarFieldEnumSchema;
