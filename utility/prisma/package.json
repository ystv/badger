{
  "name": "bowser-prisma",
  "private": true,
  "devDependencies": {
    "prisma": "^5.1.1"
  },
  "scripts": {
    "prisma:generate": "prisma generate",
    "postinstall": "yarn prisma:generate"
  }
}
